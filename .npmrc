# Use pnpm
engine-strict=true

# Prefer workspaces
prefer-workspace-packages=true

# Use exact versions for workspaces
save-workspace-protocol=rolling

# Enable automatic package-lock.json
package-lock=false

# Use npm registry
registry=https://registry.npmjs.org/

# Always publish with public access
access=public