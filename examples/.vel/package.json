{
  "name": "vel-example",
  "version": "1.0.0",
  "description": "Teloce .vel files production example",
  "private": true,
  "scripts": {
    "build": "teloce build",
    "dev": "teloce dev --proxy http://localhost:5000",
    "watch": "teloce watch"
  },
  "dependencies": {
    "teloce": "^0.3.0"
  },
  "devDependencies": {
    "@teloce/cli": "^0.3.0",
    "@teloce/vite-plugin": "^0.3.0"
  }
}